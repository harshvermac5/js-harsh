// var code (global)
var name; // it is called declaration, and is hoiseted
console.log(name); // undefined
name = "harsh" // it is called initalisation and is not hoisted 
console.log(name); // undefined
